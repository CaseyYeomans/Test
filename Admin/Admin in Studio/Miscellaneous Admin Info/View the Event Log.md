# View the Event Log

Check the Event log when attempting to troubleshoot issues, such as server connectivity problems. Errors are logged in the Application log under Windows Logs and can be accessed through the Windows Event Viewer.

When metadata is created, edited, or deleted, the events are recorded in the Windows Application Event Log.